[project]
name = "grading-cloud"
version = "0.1.0"
description = "Cloud-native grading pipeline monorepo."
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["shared", "services/*"]
